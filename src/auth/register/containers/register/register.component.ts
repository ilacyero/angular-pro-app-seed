import { Component } from '@angular/core';

@Component({
  selector: 'register',
  template: `
    <div>
      <auth-form>
      </auth-form>
    </div>
  `
})
export class RegisterComponent {
  constructor() {}
}
